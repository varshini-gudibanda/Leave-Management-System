Order Management System

An application which has two views: Employee view and Admin view
Employee can create leave requests whereas Admin can approve or reject the requests

To run the project

1) Activate Virtual environment and install all the dependencies
2) Run Python manage.py runserver(in Terminal 1)
3) Run npm run dev (in Terminal 2)
